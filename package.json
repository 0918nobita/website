{
  "name": "vision",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "yarn workspace @vision/ssg build && yarn workspace @vision/static build",
    "serve": "yarn workspace @vision/static serve",
    "lint": "yarn workspace @vision/static lint",
    "format": "yarn workspace @vision/static format"
  },
  "workspaces": [
    "workspaces/*"
  ]
}
